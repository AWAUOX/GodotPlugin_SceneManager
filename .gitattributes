# Normalize line endings for all files that Git considers text files.
* text=auto eol=lf

# Use include pattern: exclude everything first, then selectively include needed folders
# This ensures only the plugin and demo files are included when downloading from asset library

# Exclude everything first
# /*                               export-ignore
# /**                              export-ignore

# # Include the addon folder and its contents
# !/addons                         !export-ignore
# !/addons/                        !export-ignore
# !/addons/long_scene_manager      !export-ignore
# !/addons/long_scene_manager/**   !export-ignore

# # Include the demo/test folder and its contents
# !/demo_test_scene_manager        !export-ignore
# !/demo_test_scene_manager/**     !export-ignore